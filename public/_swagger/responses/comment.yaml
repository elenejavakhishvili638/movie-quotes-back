components:
  responses:
    comments:
      "201":
        description: OK
        content:
          application/json:
            schema:
              allOf:
                - $ref: "../schemas/comment.yaml#/components/schemas/Comments"
