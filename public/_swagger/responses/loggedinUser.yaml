components:
  responses:
    loggedinUsers:
      "201":
        description: OK
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - $ref: "../schemas/loggedinUser.yaml#/components/schemas/loggedinUser"
