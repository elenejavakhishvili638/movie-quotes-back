components:
  responses:
    users:
      "201":
        description: OK
        content:
          application/json :
            schema:
              type: array
              items:
                allOf:
                  - $ref: "../schemas/updateUser.yaml#/components/schemas/updateUser"
